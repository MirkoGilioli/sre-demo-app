firestore:
  database_id: "sre-demo"
  collection_name: "agenda_events"

opentelemetry:
  service_name: "agenda-backend"
  service_namespace: "sre-demo"

chaos:
  default_latency_ms: 0
  default_error_rate: 0.5
